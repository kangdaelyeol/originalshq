# Todo List

## CMIP - V1

### 화면 구조 - 통합 대시보드가 먼저

**`매체별 성과가 각각 떨어져 있으면 배분 판단이 안됨`**

- 첫 화면은 전 매체 합산이어야 합니다.

| Requirements          | 완료                                     |
| --------------------- | ---------------------------------------- |
| 매체별 성과 측정 표시 | Google api 통합 과정에서 자연스럽게 추가 |

**`기본 진입 화면을 전체(통합)으로 변경.`**

- 전 매체 합산 지출·노출·클릭·전환과 통합 CTR / CPC / CPA / ROAS를 최상단에 노출, 대시보드 디폴트 페이지는 [전체 매체 - 7일기준(당일포함) 노출 ]

| Requirements   | 완료                                     |
| -------------- | ---------------------------------------- |
| CVR 추출       | O                                        |
| CPM 추출       | O                                        |
| UI 최상단 노출 | Google api 통합 과정에서 자연스럽게 추가 |
| ROAS 계산      | 매출 데이터 추가시 같이 합산 표시        |

**`통합 화면 안에 매체별 비교 테이블 추가`**

- 행은 Meta / Naver / Google / 기타, 열은 지출·노출·클릭·전환·CTR·CPC·CPA·지출 비중(%). 비중이 보여야 예산 재배분 판단이 가능합니다

**`매체 전환은 상단 탭 버튼 방식(전체 | Meta | Naver | Google)`**

- 매체를 눌러 이동해도 선택한 기간은 그대로 유지되어야 합니다

**`연결되지 않은 매체는 탭을 숨기지 말고 비활성 상태로 표시`**

- 데이터가 없는 것인지 미연동인지 구분되어야 합니다

### KPI 누락

**`CVR (전환율)`**

- 22,305클릭 → 1,474전환이면 6.6%인데 화면 어디에도 없습니다. CPA만으로는 클릭이 문제인지 랜딩이 문제인지 분리가 안 됩니다

| Requirements | 완료                                      |
| ------------ | ----------------------------------------- |
| CVR 추출     | [중복](#화면-구조---통합-대시보드가-먼저) |
| UI 노출      | [중복](#화면-구조---통합-대시보드가-먼저) |

**`전환매출액 · ROAS`**

- 인하우스는 결국 ROAS로 보고가 올라갑니다. 매체에 안 잡히는 전환은 수 입력값과 연동, 매출액 데이터는 진헌님께 요청

| Requirements | 완료                                      |
| ------------ | ----------------------------------------- |
| ROAS 계산    | [중복](#화면-구조---통합-대시보드가-먼저) |

**`CPM`**

- CPC·CPA만 있으면 매입 단가가 오른 것인지 소재가 죽은 것인지 구분되지않습니다

| Requirements | 완료 |
| ------------ | ---- |
| CPM 추출     | O    |

**`도달 · 빈도`**

- 9/4에 노출 128,643인데 CTR이 3.3%로 떨어졌습니다. 빈도 지표가 없으면 소진 여부를 추적할 수 없습니다

| Requirements                        | 완료 |
| ----------------------------------- | ---- |
| 도달(Reach) 및 빈도(Frequency) 추출 | O    |
| Reach / Frequency 지표 UI 추가      | O    |

**`전기 대비 증감`**

- Summary 카드마다 증감률 뱃지. 절대값만으로는 좋은지 나쁜지 판단이 안됩니다

| Requirements                     | 완료 |
| -------------------------------- | ---- |
| 전가 대비 증감률 로직 및 UI 추가 | O    |

**`목표 대비 달성률`**

- 월 예산·목표 CPA를 세팅하고 진척률을 함께 표시

| Requirements                         | 완료                   |
| ------------------------------------ | ---------------------- |
| 월 예산, 목표 입력 및 계산 로직 구현 | 구매데이터 수집시 구현 |

### 표기 정합성

- 표 안 CTR이 4.69 / 3.3 / 4.6처럼 소수점 자릿수가 제각각입니다. 4.69% / 3.30% / 4.60% 형태로 통일

- CPC·CPA도 표에서는 단위가 빠져 있습니다. 값에 '원'을 붙이거나 열 헤더에 (원) 명시

- 숫자는 고정폭 서체로 자릿수를 맞춰야 세로로 비교가 됩니다

| Requirements                                                  | 완료 |
| ------------------------------------------------------------- | ---- |
| 소수점 정밀도(precision) 관련 수치 표시 개선 및 단위(원) 추가 | O    |

### 집계 로직 오류 - 지표 추가보다 우선

**`당일 데이터가 그대로 합산`**

- 9/9 노출이 26,857로 다른 날의 3분의 1 수준인데 합계와 평균에 그대로 섞여 CPC가 768원으로 튀어 있습니다. 기본 조회 기간을 어제까지로 잡고, 당일을 포함할 경우 집계 중임을 표시해주세요.

| Requirements                  | 완료 |
| ----------------------------- | ---- |
| 당일 집계 기록 분리 로직 구현 | O    |

**`기간 버튼 기준 명시`**

- '최근 7일'이 오늘을 포함한 7일인지 어제 기준 7일인지 화면에서 바로 읽히지 않습니다.

| Requirements                   | 완료 |
| ------------------------------ | ---- |
| UI 입력(버튼) 의미 명확히 표시 | O    |

### 그래프 · UI 접근성

**`데이터 라벨 상시 노출`**

- 꺾은선은 포인트 위, 막대는 막대 위에 값 표시. 라벨이 겹치면 자동으로 간격 조정

| Requirements          | 완료 |
| --------------------- | ---- |
| 라벨 상시노출 UI 구현 | O    |

**`이중축 조합 차트를 기본형으로`**

- 막대는 지출, 꺾은선은 CPA 또는 전환. 따로 보면 '돈을 더 썼는데 효율이 어땠나'가 한 화면에 잡히지 않습니다

| Requirements                             | 완료 |
| ---------------------------------------- | ---- |
| 막대 및 꺾은선 UI 같이 볼 수 있도록 분리 | O    |

**`기타 사항`**

- 지표 체크박스 토글로 원하는 축만 켜고 끄기

- 호버 툴팁에 해당 일자의 전체 지표를 묶어서 표시

| Requirements       | 완료 |
| ------------------ | ---- |
| 지표 토글박스 구현 | O    |

## Monday CRM - xTool lead Manager ETL

### Monday Requirements

**`요청 사항`**

- 모든 보드에 생성 로그 / 아이템 ID
- 고객명 - 이름 / 비고(회사 또는 직책 기입용) 분리 필요

**`요구 사항`**

- 녹음 진행 시작일 (언제 부터 영업시 녹음 진행을 시작 했는지)

**`방문체험 보드 관련`**

- (예약) 체험 기기 컬럼 분할 -> 기기(레이블) / 울산 체험 여부(체크) / 비고(특이사항) ex) M2의 경우 ‘잉크젯 모듈 체험 불가’ 등 작성용
- 보류 시작일은 없고 보류 종료일은 있음
- 콜백 전단계는 기입 안되어 있는데 콜백 그 이후단계부터 기록됨
- 담당자 컬럼 - 수동 변경 불가능하여 CS팀에서 ‘메모’ 컬럼에 수기로 입력중

**`주말방문체험 보드 관련`**

- (예약) 체험 기기 컬럼 레이블화 되어있는데, 방문체험 보드는 레이블화 안돼 있음
- 담당자 컬럼 레이블 가능. 방문체험 보드는 수동 설정할 수 없음.
- 보류 시작일은 없고 보류 종료일은 있음
- 콜백 전단계는 기입 안되어 있는데 콜백 그 이후단계부터 기록됨

방문체험 보드에 존재하지만 주말 방문체험에 존재하지 않는 필드

- 유입 경로
- 선샘플 진행 여부 (체크)
- 녹음 진행 여부 (체크)

**`방문체험 후 만족도 조사 보드 관련`**

- 아이템필드 - 이름(기업명), 전화번호 같이 입력됨 -> 이름 / 전화번호 / 비고(기업 또는 직책) 컬럼 분리 추천
- “구매 또는 구매 예정 시 가장 중요하게 생각하는 요소는 무엇인가요?” 컬럼이 두 개 중복되어 있음(레이블 선택 / 자유 입력) 컬럼 이름이 같음
- “구매확률” 컬럼 레이블: 기기 사용 교육 / 재방문 예정 레이블은 “확률” 과 관련 없기 때문에 분리 필요.
  - ex) 재방문 예정 -> 재방문 여부 컬럼(체크 컬럼)
  - 기기 사용 교육 -> 기기 사용 교육 여부 컬럼(체크 컬럼)

**`전체 오프라인 영업 보드 관련`**

- 어떠한 목적으로 사용되고 있는가?
- 어떠한 기준으로 데이터(row)가 자동 생성?
- 전화번호 컬럼이 2개있는데 어떠한 기준으로?
- 유입경로 / 완료상태는 이미 보드로 추적 가능한데, 어떠한 기준으로 수동으로 셋팅하게 두었는가?
- 연결된 아이템의 기준을 ‘이름’ -> ‘전화번호’ 이동 추천(고유성)

**`오프라인 결제 보드 관련`**

- “구매경로” 컬럼이 비어있은 경우: AS / 분할납부 / 기타내역 / 방문설치 같은 경우 비어있음. 이에 대한 레이블 대책이 있었는가, 비어있는게 의미있다면 어떠한 기준인가, “기타오프라인” 레이블과 똑같이 봐도 되는 것인가
- “수령방식” 컬럼이 비어있는 경우

**`제품 예약 고객 명단 보드 관련`**

- 오프라인 결제 보드 -> 이름 컬럼으로 데이터 복사하고 있음, 이를 전화번호 기준으로 변경 필요 - ‘고객명’ / ‘오프라인
- 결제’ 컬럼이 중복되며, 이름도 수기로 입력해야함, 전화번호도 마찬가지

**`택배 발송 보드 관련`**

- “문자 발송하기” 컬럼: 발송 이전 / 대기 구분 기준?
- “온라인 / 오프라인” 컬럼: 입력 누락 방지를 위해 default(빈 값)제거후, 오프라인을 기본값으로 하는 것을 추천

**`샘플테스트 보드 관련`**

- ‘상태’ 컬럼: ‘상태’ 레이블 사용 용도?

**`출장업무 보드`**

- 클라이언트 컬럼 -> 전화번호 / 고객명 / 비고(직책, 회사 등) 컬럼으로 분리
